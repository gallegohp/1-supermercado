spring:
  application:
    name: supermercado
  datasource: 
    url: jdbc:mysql:gondola.proxy.rlwy.net:46263/railway
    username: root
    password: ckRyWcmrnZNceHxxmyloJxyzeTRmpCAR
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:  
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    database-platform: org.hibernate.dialect.MySQLDialect

jackson:
  serialization:
    indent-output: true

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

server:
  port: 9091